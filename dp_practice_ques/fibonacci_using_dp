#include <bits/stdc++.h>

using namespace std;
const int N=1e5+10;

int fib(int num){
    if(num==0)return 0;
    if(num==1)return 1;
    return fib(n-2)+fib(n-1);
}
int main()
{
 int n=5;
 cout<<fib(5);
    return 0;
}
