#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main() {
	// your code goes here
	ll t;
	cin>>t;
	while(t--){
	    ll n,k;
	    cin>>n>>k;
	    
	    ll len=pow(2,n);
	    ll ans=min(2*k,len)*(len-1);
	    cout<<ans<<endl;
	}
	return 0;
}
