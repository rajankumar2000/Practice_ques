#include <bits/stdc++.h>
#define ll long long int
using namespace std;

int main() {
	ll t;
	cin>>t;
	while(t--){
	    ll n,m;
	    cin>>n>>m;
	    ll a[n];
	    set<ll> s;
	    for(ll i=0;i<n;i++){
	        cin>>a[i];
	        s.insert(a[i]);
	    }
	    if(s.size()<m){
	        cout<<"yes"<<endl;
	    }else{
	        cout<<"no"<<endl;
	    }
	}
	return 0;
}
